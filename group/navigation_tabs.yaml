  dashboard:
    name: "Dashboard"
    view: yes
    entities:
    - group.today
    - group.scenes
    - group.devices
    - group.batteries
    - group.upcoming_events
    - group.bitcoin
    - group.user_presence
    - group.google_calandars

  lights_view:
    name: "Lights"
    view: yes
    entities:
    - group.living_room_lights
    - group.office_lights
    - group.kitchen_lights
    - group.scenes
    - group.scripts_lights
    - group.led_lights
    - group.ambient_lights
    - group.living_room_overhead_lights
    - group.overhead_lights

  enviroment:
    name: "Enviroment"
    view: yes
    entities:
    - group.motion_sensors
    - group.humidity_sensors
    - group.temperature_sensors
    - group.weather_current
    - group.weather_summary
    - group.weather_details

  media:
    name: "Media"
    view: yes
    entities:
    - group.vtv
    - group.entertainment_center
    - group.media_scenes
    - group.ambivision

  network:
    name: "Network"
    view: yes
    entities:
    - group.status_alert
    - group.server_status
    - group.downloads
    - group.connection_speed
    - group.cloud_status
    # - group.pihole
    - group.iot_status

  hass:
    name: "Home Assistant"
    view: yes
    entities:
    - group.tools
    - group.options_lights
    - group.options_media
    - group.options_notify2
    - group.options_environment
    - group.scripts_lights
    - group.scripts_lights_test
    - group.stored_variables

  all:
    name: "All"
    view: yes
    entities:
    - group.all_automations
    - group.all_covers
    - group.all_devices
    - group.all_lights
    - group.all_scripts
    - group.all_switches
